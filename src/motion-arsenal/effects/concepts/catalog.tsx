import React from 'react';
import type { EffectEntry } from '../../types';
import { ConceptEffectPreview } from './ConceptEffectPreview';

const entries = [
  ['Bayer Dither Dissolve', 'Bayer-geditherter Abschnittswechsel mit Gold-Kante.', 'Canvas ImageData auf reduzierter Auflösung.'],
  ['Inverse Glow Cursor', 'Invertierter Cursor-Spot mit nachziehendem Akzent-Ring.', 'CSS mix-blend-mode und transform-only rAF.'],
  ['Lenticular Tilt', 'Interlaced Multi-Frame-Bild reagiert auf Tilt und Pointer.', 'WebGL Textur-Slices.'],
  ['Elastic Lag Grid', 'Gestaffeltes Grid folgt der Scrolltiefe mit elastischem Lag.', 'CSS Vars und transform-only rAF.'],
  ['Glyph Matrix Rain', 'Deterministischer Glyphen-Regen als Terminal-Textur.', 'Canvas-Spalten mit PRNG.'],
  ['Kinetic Twist Typo', 'Drehende Display-Typografie für große Marken-Statements.', 'SVG-Skeleton, optionaler Shader.'],
  ['Path-Follow Hero Scroll', 'Hero folgt einer SVG-Kurve durch die Scroll-Choreografie.', 'SVG path lookup und transforms.'],
  ['Progressive Blur Stack', 'Fokus-Stack mit abgestuften Blur- und Opacity-Ebenen.', 'CSS @property und backdrop-filter.'],
  ['Magnetic Field Cards', 'Karten ziehen sich in einem begrenzten Feld zum Cursor.', 'Distanz-basierte Transform-Offsets.'],
  ['Scroll-Synced Typo Background', 'Große Marken-Typografie rotiert subtil mit der Scrolltiefe.', 'CSS Scroll-Driven Animations mit Fallback.'],
  ['Aurora Borealis Background', 'Ruhige, goldgesäumte Gradient-Atmosphäre ohne Canvas.', 'Transform-animierte Gradient-Blobs.'],
  ['Scroll-Driven CSS Reveal', 'Progressive In-View-Reveals mit nativen Scroll-Timelines.', 'animation-timeline: view() und IO Fallback.'],
  ['Lenticular Scroll Image', 'Scroll-synchroner Interlace-Wechsel durch vorbereitete Bildframes.', 'Sprite-Strip und CSS-Maske.'],
  ['Backdrop Blur Grain Overlay', 'Material-Overlay aus Backdrop Blur, Grain und Goldkante.', 'backdrop-filter mit statischem SVG-Grain.'],
  ['View Transition API Cross-Fade', 'Dunkelraum-optimierter nativer Crossfade für DOM-Zustände.', 'document.startViewTransition mit Fallback.'],
  ['GSAP Flip Gallery Morph', 'Galerie-Kachel wächst per FLIP in den Detail-View.', 'FLIP transforms mit Fokus-Dialog-Fallback.'],
  ['Holographic Type Effect', 'Irideszente Gold-Typografie mit kontrollierter Lichtmaske.', 'background-clip und CSS masks.'],
  ['Dithered Data Heatmap', 'Dichtebasierte Heatmap im Bayer-Print-Look.', 'DOM-Zellen mit SVG Pattern.'],
  ['Infinite Parallax Loop', 'Entkoppelter Endlos-Loop mit Parallax-Faktoren.', 'GSAP Observer Proxy-Scroll.'],
  ['Grid Blind Mask Reveal', 'Gestaffelte Grid-Maske legt die neue Ebene frei.', 'SVG Mask mit deterministischem Stagger.'],
  ['Polaroid Stack Scroll', 'Gestreute Polaroids ordnen sich zu einem Stack.', 'Transform-, blur- und opacity-Scrub.'],
  ['Horizontal Pin Gallery', 'Gepinnte horizontale Bilderstrecke mit Fortschrittsrail.', 'ScrollTrigger pin/scrub mit vertikalem Fallback.'],
  ['Layered Zoom Dolly', 'Mehrere 2D-Ebenen erzeugen eine räumliche Kamerafahrt.', 'CSS perspective und translateZ.'],
  ['SVG Metric Graph Draw', 'Eine Kennzahlenkurve zeichnet sich beim Scrollen.', 'SVG stroke-dashoffset.'],
  ['Tumbler Vault OTP', 'Mechanische OTP-Eingabe mit rollenden Ziffern.', 'CSS transform-Räder, Paste und Keyboard Support.'],
  ['Peel Reveal Modal', 'Eine aufrollende Ecke enthüllt einen Dialog.', 'Gradient-Curl und CSS transforms.'],
  ['Canvas Line Typography', 'Linien-Ornamente bilden eine technische Marken-Wordmark.', 'Canvas einmalig bei Mount und Resize.'],
  ['Squiggly Distortion Text', 'Feine SVG-Displacement-Wellen beleben Headlines.', 'feTurbulence und feDisplacementMap.'],
  ['Dotted World Map Connect', 'Dekorative Punktkarte mit animierten Verbindungspfaden.', 'Vorgerendertes SVG-Punktfeld und CSS Arcs.'],
  ['3D Circle Text Scroll', 'Headline-Wörter stehen auf einem 3D-Kreisring, der sich beim Scrollen dreht; die Verzerrung der Typo folgt der Scroll-Geschwindigkeit — kinetische Typo als Raum-Erlebnis. NOX-Variante der Codrops-„On-Scroll 3D Circle Text Animation" (Three.js + Shader).', 'WebGL (Three.js) + Shader: Wörter werden per MSDF-Textur auf einen 3D-Kreisring gelegt (drei-msdf-text-utils-Pattern), Rotation ist an Scroll-Progress gescrubbt; ein Vertex-/Fragment-Shader verzerrt die Glyphen geschwindigkeitsabhängig (Offset-Noise entlang der Ring-Tangente). Determinismus: Wort-Offsets/Phasen via `det(i, salt)`; kein Math.random. Shader folgt.'],
  ['3D Rotating Scroll Images', 'Bilder rotieren beim Scrollen auf der Y-Achse durch den Raum (Perspective-Karussell) — Galerien bekommen Tiefe, jedes Bild ‚dreht sich in den Blick\'.', 'Perspective-Container, Bilder mit rotateY-Fortschritt via scroll-driven animation-timeline; Übergang über Midpoint. transform-style: preserve-3d; backface-visibility hidden.'],
  ['3D Scroll Text Parade', 'Headline-Zeilen marschieren beim Scrollen gestaffelt in den Raum: jede Zeile dreht aus rotateX/-Z-Haltung in die Leseposition (Perspective-Bühne, CSS-first).', 'Perspective-Container; jede Zeile translateZ- bzw. rotateX-keyframed via scroll-driven animation (animation-timeline: view()) — native CSS, kein Scroll-JS. Zeilen mit will-change: transform.'],
  ['Anaglyph Depth Hover', 'Retro-3D-Tiefe beim Hover: Das Bild spaltet sich in zwei leicht versetzte, gold-/cyangetönte Ebenen, die gegenläufig dem Pointer folgen — der Look alter 3D-Brillen als elegante Hover-Micro-Interaction (NOX-Variante des Anaglyph-Hover-Trends).', 'JS+CSS: zwei Bild-Kopien (grayscale + Tint via filter), `mix-blend-mode: screen`, translate-Offsets aus Pointer-Position (geclampt, entgegengesetzte Richtung), transform-only im rAF — nur bei Hover aktiv. Ohne Pointer: statischer 3D-Tilt als Fallback. Deterministisch, kein Zufall.'],
  ['ASCII Portrait', 'Bilder werden in goldene ASCII-Zeichen übersetzt: Ein feines Raster (80×45) mappt die Luminanz jedes Pixels auf eine Zeichen-Rampe (`. :-=+*#%@`), det()-Jitter bricht harte Kanten. Der Aufbau tippt sich zeilenweise beim Scrollen — Terminal-Ästhetik als Premium-Bildbehandlung.', 'Canvas 2D: Originalbild einmal auf Rastergröße (z. B. 80×45) in ein Offscreen-Canvas zeichnen, `getImageData` auslesen, Luminanz → Zeichen-Rampe; pro Zelle `fillText` in Monospace (deterministischer Jitter via det()-PRNG für Abwechslung). Einmaliges Rendering; nur die Zeilen-Enthüllung animiert (rAF nur während des Reveals). Gold auf Dark, dezenter Glow für helle Zeichen.'],
  ['Audio Waveform Visualizer', 'Equalizer-Balken als goldene Klang-Architektur: Ohne Audio spielt ein deterministischer Pseudo-Wave (det()-PRNG), mit AudioContext/`AnalyserNode` tanzen die Balken zum echten Signal. Optional gespiegelt (Mittellinie) mit Glow-Kappen und Abkling-Smoothing — Player-/Podcast-Premium.', 'Canvas 2D: `bars` (z. B. 48) vertikale Balken, Höhe pro Frame aus `AnalyserNode.getByteFrequencyData` (optional) oder aus `det(i, t)`-Pseudo-Wellen (Sinuskombination + PRNG-Jitter, kein `Math.random`). Balken mit Gold-Gradient + `shadowBlur`-Kappen; Decay: Höhe gleitet mit `max(prev * decay, new)`. rAF läuft nur, solange sichtbar/aktiv (IntersectionObserver + `audio.playing`).'],
  ['Aurora Beam Drift', 'Mehrere konische Gold-/Bernstein-Balken driften langsam über dunklen Grund, weich mit der Seite verschmelzend — Signature-Background für Hero/CTA (NOX-Variante von Magic UI Aurora).', '3-4 rotate()-Balken mit großem radialem/conischem Verlauf (mix-blend-mode: screen), blur(60px) auf Parent; Drift als 20–30s CSS-Keyframes (translate + rotate, leicht unterschiedliche Durations). Kein JS, keine Canvas.'],
  ['Ball Pit Sinkhole Reveal', 'Kugelgrube als Cursor-Reveal: Maus bewegt Kugeln (det-PRNG-Farben), ein Sinkhole folgt dem Cursor und gibt den Gold-Boden frei.', 'Canvas 2D: Kugeln als deterministische Partikel (det-PRNG), Sinkhole-Radius um Cursor-Position, Kugeln versinken mit Tiefen-Fade.'],
  ['Black Mirror Cracked Text', 'Black-Mirror-Opener: Die Headline zerbricht in scharfkantige Splitter (CSS-clip-path-Polygone), die kurz auseinanderfahren und wieder einschnappen — dramatischer Glitch-Intro.', 'Text 4–6x als absolut positionierte Kopien mit clip-path-Polygonen (Slices); Split-Animation: translate + rotate je Fragment mit gestaffelten Delays, Rückkehr mit Elastic-Bezier.'],
  ['Blurry VHS Image Filter', 'Retro-VHS-Look als Overlay-Filter: leichte Unschärfe + Chroma-Shift + wandernde Scanlines über Bilder — Nostalgie-Layer für Galerien in NOX-Gold-Ton.', 'CSS-Filter-Kette (contrast/saturate/hue-rotate) + zwei Overlay-Layer: SVG feTurbulence-Scanlines (animiert via backgroundPosition) und Chroma-Shift per mix-blend-mode.'],
    ['Button Border Glitch', 'Button-Rahmen glitcht beim Hover: Die Border zerfällt kurz in versetzte Farb-Segmente (Gold/Rot/Blau-Channel), Text wackelt synchron — retro-digitaler CTA.', 'CSS-only: Border als 2px Gradient-Layer mit maskierte Segmenten (repeating-linear-gradient + animation der mask-Position); Text-Duplikat mit 1px translate für Chroma-Effekt.'],
  ['Card Fan Deck', 'Ein Kartendeck fächert sich beim Hover/Fokus auf: jede Karte bekommt einen deterministischen Winkel und Versatz (`det(i, salt)`), Klick dealt die oberste Karte um (Flip + Slide). Gold-Kanten auf dunklen Rückseiten, rein Transform/Transition.', 'CSS-3D: Deck-Container mit `perspective`, Karten stapeln sich mit `translateZ`-Staffelung. Hover/Fokus auf dem Deck setzt `is-open`: Karten rotieren um die Fächer-Achse (`rotateY`/`rotate` mit `transform-origin` unten) — Winkel, Versatz und Reihenfolge deterministisch aus `det(i, salt)` beim Rendering. Klick auf die oberste Karte: `rotateX`-Flip + Slide nach vorn (State), danach wandert sie ans Stapel-Ende. Keine rAF-Schleife, nur Transitions.'],
  ['Cinema Letterbox Modal', 'Video- oder Bild-Content öffnet sich als Kinomoment: Schwarze Letterbox-Balken fahren von oben/unten ein, der Content zoomt aus der Thumbnail-Position in die Mitte, ein Gold-Rahmen fasst die Bühne — Modal als Theater (NOX-Variante des Letterbox-Trends).', 'JS+CSS: Overlay mit zwei Balken (`scaleY` 0→1, transform-origin top/bottom), Content-Scale aus dem Thumbnail-Rect (FLIP-lite: eine einmalige Transform, keine Library), `backdrop-filter: blur` + Gold-Rahmen; Close per Tap/Escape kehrt die Reihenfolge um. `aria-hidden` auf dekorativen Balken.'],
  ['Clipped Section Stack', 'Sektionen liegen als Kartenstapel übereinander und klappen beim Scrollen nacheinander auf (clip-path inset) — der Seitenwechsel wird zum physischen Aufdecken.', 'Sticky-Container, darin gestapelte Panels; jede Panel-Animation via clip-path: inset() + scale, getriggert durch scroll-driven animation-timeline. Letzte Panel normal scrollt raus.'],
  ['Connected Grid Pulse', 'Beim Wechsel in eine System-Sektion verbindet sich ein Punkt-Grid mit Gold-Linien; Pulse wandern über die Verbindungen — Datenfluss als Section-Transition.', 'Zwei Raster (Knoten) + SVG-Verbindungslinien; Linien zeichnen sich per stroke-dashoffset ein, Pulse (kleine Kreise) laufen auf den Pfaden mit CSS-Offset-Pfad (offset-path) — deterministisch, kein Canvas.'],
  ['Container Scroll SplitText', 'Eine scrollbare Container-Sektion treibt eine SplitText-Timeline: Wörter/Zeilen steigen gestaffelt aus dem Container heraus — Scrollen wird zum Regie-Pult (GSAP ContainerAnimation).', 'GSAP ScrollTrigger + ContainerAnimation: SplitText zerlegt Headline in Spans; Container-Scroll-Fortschritt scrubbt die Timeline (stagger + rotateX). Kein eigenes per-Frame-JS; Scroll-Proxy nur via GSAP.'],
  ['Context Aware Fixed Emblem', 'Ein fixiertes NOX-Emblem (Logo/Orb) reagiert „kontextbewusst" auf das Scrollen: Kollidiert es mit Text/Sektionen, weicht es animiert aus — schrumpfen, wegdrehen, blurren, zerlegen oder beiseiteschieben. NOX-Variante der Codrops-„Context-Aware Animations for Fixed Elements".', 'CSS+minimal JS: Emblem `position: fixed`; IntersectionObserver (oder scroll-driven `animation-timeline`) erkennt kollidierende Sektionen und schaltet einen Reaktions-Modus (`scale-down` / `rotate-away` / `blur` / `split` / `push-up`). Alle Reaktionen als transform-/filter-Transitionen, Phasenwechsel deterministisch (kein Zufall beim Zerlegen: Split-Offsets via `det(i, salt)`).'],
  ['Counter Preloader', 'Awwwards-Style-Preloader: Eine große Gold-Ziffer zählt 0→100, darunter läuft ein feiner Progress-Balken; bei 100 klappt der Loader per Clip-Path-Wipe weg und gibt die Seite frei. NOX-Variante der klassischen Zähler-Preloader.', 'CSS+minimal JS: Zähler als `@property --n` (0→100 interpolierbar) mit `counter-reset`/`counter()` im Pseudo-Element — alternativ 3 Zeilen JS (rAF-Intervall, gedämpftes Easing). Der Wipe nutzt `clip-path: inset()` auf dem Loader-Overlay. Gold-Ziffer mit leichtem Glow (text-shadow-Stapel), Balken `transform: scaleX` (Compositor).'],
  ['CRT Screen Text', 'Text auf einem alten CRT-Monitor: RGB-Aperturgrille, horizontales Wobble und kurze Glitch-Aussetzer — Terminal-/Retro-Signatur mit NOX-Goldphosphor.', 'CSS-only: Aperturgrille als repeating-linear-gradient-Overlay (mix-blend-mode), Text-Wobble über translateY-Keyframes mit unregelmäßigen Steps, Glitch-Slices via ::before/::after mit clip-path.'],
  ['Cube Spin Route Transition', 'Seiten-/Sektionswechsel als 3D-Würfel: Die alte Ansicht dreht um die Y-Achse weg, die neue kommt von hinten in den Blick — ein physischer, räumlicher Wechsel für Portfolio- und Case-Study-Flows.', 'CSS 3D: zwei Panels in preserve-3d-Container, `rotateY` 0→-90 / 90→0 mit Feder-Easing; Route-Wechsel triggert Klassen-Toggle (React), Fokus-Management + `aria-live` für Screenreader; Fallback: Fade-Slide ohne 3D. `backface-visibility: hidden`, Gold-Kante an der Drehachse.'],
  ['Distorted Button Image Reveal', 'Button-Hover enthüllt ein Bild hinter dem Text — mit weicher Distortion (SVG-Displacement), die beim Öffnen nachlässt: Text bleibt lesbar, Bild \'bricht durch\'.', 'JS+CSS: Bild als absolut positionierter Layer mit SVG feTurbulence/feDisplacementMap; scale-Transition von .2→1 + Displacement-Amplitude 30→0 via CSS-Variable.'],
  ['Dynamic Palette Gradient', 'Hintergrund-Gradient, der die Farbpalette live aus dem DOM zieht: dominante Sektion-Farben werden als Gold-kompatible Verläufe gespiegelt.', 'JS: Palette-Extraktion (Canvas-Sampling beim InView), CSS-Vars → linear-gradient, weiche Übergänge via transition.'],
  ['Dynamic Tooltip Fragments', 'Tooltips erscheinen nicht als Block, sondern als Fragment-Reveal: mehrere Slices/Teile fliegen gestaffelt zusammen (z. B. bei Data-Punkten, Diagrammen, Features).', 'Tooltip-Inhalt in 4-8 Fragment-Spans zerlegt; jedes Fragment bekommt deterministische (det-PRNG) Verzögerung + eigene Richtung, CSS-Keyframes. Position via transform, keine Layout-Animation.'],
  ['Frame Repeat Image Transition', 'Beim Klick auf ein Grid-Bild fliegen mehrere „Frames" (Kopien desselben Bilds) entlang eines Pfads zum Detail-Panel, das sich per Clip-Path öffnet — wie ein Daumenkino, das das Bild zur Vergrößerung trägt. NOX-Variante der Codrops-„Repeating Image Transition".', 'JS+CSS: Klick-Item erzeugt N „Mover"-Elemente mit demselben Hintergrundbild (background-image), die per transform (translate + rotate + scale) gestaffelt von der Grid-Position zum Panel-Fokus fliegen; Clip-Path öffnet danach das Ziel-Panel. Pfad linear oder Sinus-Welle, Rotation/Wobble deterministisch über `det(i, salt)` — kein Math.random.'],
  ['Glitch Color Flash Text', 'Canvas-Glitch-Text mit deterministischen Farbblitzen: RGB-Slices + kurze Gold-/Cyan-Flash-Bursts auf Knopfdruck oder in Schleife — Alarm-Ästhetik für System-Meldungen.', 'Canvas 2D: Text als Offscreen-Bitmap, Slices mit det()-PRNG verschoben, RGB-Channel-Split per globalCompositeOperation; Flash = kurzer Alpha-Burst mit fillRect.'],
  ['GLSL Image Glitch', 'Shader-basierter Bild-Glitch: RGB-Channel-Split + horizontale Block-Slices + Rausch-Flakes direkt im Fragment-Shader (drei.js) — der \'kaputte Kanal\'-Look in NOX-Gold.', 'WebGL/GLSL: eine Textur, drei Channel-Offsets (r/g/b) mit det()-Noise, Block-Slices über step()-Rauschen, Flakes als randomisierte Streifen; Intensität über uniform.'],
  ['Gold Edge Flip Card', 'Karte flippt beim Hover/Fokus in 3D (rotateY) und zeigt die Rückseite — mit Gold-Glow-Kante, die beim Flip mitwandert, und Titel-Text, der beim Drehen über der Karte zu schweben scheint. NOX-Variante des klassischen 3D-Flip-Cards.', 'CSS-only: `perspective` auf dem Wrapper, `transform-style: preserve-3d`, Vorder-/Rückseite mit `backface-visibility: hidden`; Flip als rotateY-Transition mit Feder-Easing. Gold-Kante als Border + Glow auf der Rückseite (box-shadow-Stapel), Titel auf einer eigenen translateZ-Ebene über der Karte („hovering text"). Hover + `:focus-visible` + Touch-Tap (JS-Klasse) lösen den Flip aus.'],
  ['Gooey Cursor Blob', 'Zwei Cursor-Follower verschmelzen durch SVG-Gooey-Filter zu einem zähen Gold-Blob, der über UI-Elemente ‚klebt\' — dekorativer Cursor-Charakter ohne Performance-Last.', 'SVG-Filter (feGaussianBlur + feColorMatrix) als Gooey-Sieb auf einem Wrapper; 2 Follower (Rückkopplung mit unterschiedlichen Lerp-Faktoren) via rAF-Pointer-Tracking — nur Position, keine Partikel.'],
  ['Grid Sliced Hover', 'Beim Hover zerfällt eine Karte/Visuelle in horizontale Slices, die gestaffelt auffächern und die nächste Ebene freigeben (Quai-Network-Stil, NOX-gebaut).', 'Bild in N Slice-Spans mit background-position gestückelt; Hover: translateY + skew gestaffelt (transition-delay via det-PRNG). Pure CSS, transform-only.'],
  ['Heat Haze Shimmer', 'Hitzeflimmern über Bildern: ein SVG feTurbulence/Displacement-Filter wellt einen schmalen horizontalen Streifen, der langsam über die Fläche wandert — wie Luft über Asphalt.', 'SVG-Filter (feTurbulence + feDisplacementMap) auf einem Overlay-Duplikat des Bilds; nur der Streifen wird displaciert (Masken-Gradient), animiert über baseFrequency/seed-Switch.'],
  ['Hero Book Cover Intro', 'Hero-Section öffnet sich wie ein Buch: Cover-Cover kippt auf, Gold-Buchrücken glänzt, Inhalt entfaltet sich darunter.', 'CSS 3D-Perspektive: Buch-Deckel (RotateY + transform-origin), Seiten-Stapel als gestaffelte Ebenen, Gold-Rücken-Sheen.'],
  ['Horizontal Accordion Gallery', 'Eine Zeile aus 4–6 Bild-Panels: Beim Hover/Fokus expandiert ein Panel auf Kosten der anderen (flex-grow-Akkordeon), die Caption gleitet herein, schmale Gold-Trennlinien bleiben sichtbar — Premium-Galerie statt Slideshow.', 'CSS-only: Flexbox-Container, jedes Panel `flex-grow: 1`, aktiv `flex-grow: 4–6` mit `transition: flex-grow` (nur Layout-Eigenschaft, günstig bei wenigen Panels). Caption liegt absolut unten, `opacity` + `translateY`-Transition, `pointer-events: none`. Tastatur: `:focus-visible`/`:focus-within` expandiert dasselbe Panel. Gold-Trenner als `border-left` mit `#d4af37`-Anteil.'],
  ['Iron Filings Magnet Transition', 'Bild-Transition wie Eisenfeilspäne am Magneten: Das alte Bild zerfällt in deterministische Partikel, die zum neuen Bild hinüberwandern und sich dort sammeln — kinetisch & hypnotisch.', 'Canvas 2D: beide Bilder als Offscreen-Bitmaps, N Partikel mit Start-Position (det-PRNG) + Ziel-Position (Brightness-Sampling des Zielbilds); Übergang = easede Interpolation mit leichter Magnet-Krümmung.'],
  ['Ken Burns Cinematic Slideshow', 'Hero-Bilder gleiten im Kino-Look: jedes Bild zoomt/pannt langsam (Ken-Burns-Keyframes), wechselt per Crossfade und liegt unter einer dezenten Gold-Vignette — NOX-Variante des klassischen CSS-Ken-Burns-Effekts.', 'CSS-only: je Slide `@keyframes` mit `scale3d`/`translate3d` (unterschiedliche Zoom-Pfade: rein, raus, diagonal), Crossfade über gestaffelte Opacity, Gold-Vignette als radial-gradient-Overlay. Kein JS nötig; Dauer und Richtung je Slide deterministisch über Props, Loop via `animation-delay`-Kette.'],
  ['Labyrinth Balance Toggle', 'Toggle als Labyrinth-Kugel: Der Schalter ist ein Mini-Irrgarten, die Gold-Kugel muss zur Ziel-Position balanciert werden (Hover-Tilt oder Drag).', 'CSS 3D Tilt + Physik-Simulation (leicht): Kugel-Rolling aus Tilt-Winkeln, Pfad-Wände als CSS-Shapes, Ziel = „ON“.'],
  ['Liquid Distortion Card', 'Karten-Inhalt wellt sich beim Hover wie eine Flüssigkeit: SVG-Turbulenz-Filter verschiebt die Darstellung in einer sanften Welle — Hover wird haptisch.', 'SVG-Filter (feTurbulence + feDisplacementMap) mit Animations-<animate> auf baseFrequency/seed; Hover skaliert displacement in einem rAF-Fade. Fallback ohne Filter bei prefers-reduced-motion.'],
  ['Looking Through Water Blur', 'Bilder wirken, als läge Wasser davor: weiche, wandernde Blur-Pools (Linsen-Vergrößerung) mit sanfter Bewegung — 5 Intensitäts-Styles von \'Regentropfen\' bis \'Tiefsee\'.', 'JS+CSS: 3–6 Blur-Kreise (backdrop-filter: blur + radial-gradient-Maske) mit det()-Bahnen und Sinus-Bewegung; Styles = unterschiedliche Radien/Blur/Anzahl.'],
  ['Magnetic Grid Navigation', '3×3-Navigationsgrid, dessen Zellen magnetisch dem Cursor folgen (nichtlineare Anziehung, Lusion-Stil) — Menü wird zur haptischen Fläche.', 'Je Zelle translate-Offset aus Pointer-Distanz mit ease-out; Stärke per prop geclampt, Zentrum-Zelle stärker. Transform-only, rAF gebündelt (eine Animation pro Frame).'],
  ['Marquee Page Border Scroll', 'Marquee als Seitenrahmen: Gold-Text läuft beim Scrollen um die Viewport-Kanten, scrollt mit und wechselt die Richtung an den Ecken.', 'CSS Marquee (translateX-Loop) + Scroll-Link: Rahmen-Position aus Scroll-Y, Richtungswechsel an Ecken via Waypoints.'],
  ['Mesh Gradient Flow', 'Fließendes Mesh-Gradient-Feld: 3–5 radial verlaufende Farb-Blobs driften als geblurte Layer über dunklem Grund und drehen langsam den Farbton — NOX-Variante in Gold/Amber-Signatur (#d4af37), rein CSS, ohne Canvas.', 'CSS-only. Zwei bis drei `::before/::after`-Layer mit großen `radial-gradient`-Blobs (Gold `#d4af37`, Bernstein, tiefes Braun) und `filter: blur(...)` driften über `transform`-Keyframes; ein optionaler, sehr langsamer `hue-rotate` auf einem separaten Layer erzeugt den „Fluid"-Charakter. Kein JS, keine rAF-Kosten — nur Compositor-Transforms + einmaliger Blur.'],
  ['Morphing Blob Field', '3–5 autonome Blobs morphing organisch über animierte `border-radius`-Keyframes (8-Wert-Syntax) und rotieren langsam — gestaffelte Phasen, Gold-/Bernstein-Töne, stark geblurt. Ambient-Background, kein Cursor-Bezug (Abgrenzung zu Gooey Cursor Blob).', 'CSS-only. Jeder Blob ist ein `div` mit großen, unregelmäßigen `border-radius`-Werten (z. B. `41% 59% 41% 59% / 53% 51% 49% 47%`), die per Keyframes zu weiteren Formen interpolieren; zusätzlich dreht sich der Blob langsam (`rotate`), was die Morphing-Wahrnehmung verstärkt. Blobs sind geblurt und liegen unter dem Content (`z-index`), `pointer-events: none`. Phasen/Positionen deterministisch per `det(i, salt)`-PRNG beim Rendering berechnet.'],
  ['Pixelated Scroll Load', 'Bilder starten beim Scrollen bewusst pixeliert und schärfen sich zeilenweise — Laden wird als Choreografie inszeniert statt versteckt.', 'image-rendering: pixelated auf ein <canvas>-Proxy? NOX-Variante: Bild in N Querstreifen, jeder startet mit scale/translateY + filter: blur(2px), scharf per scroll-driven animation-range-Staffelung. CSS-only.'],
  ['Radar Sweep Glitch Button', 'Button mit Radar-Charakter: Beim Hover sweep-t eine glitchige Scanlinie über die Fläche, gefolgt von kurzen RGB-Überlagerungen — CTA mit \'System-aktiv\'-Gefühl.', 'CSS-only: Scanline als ::before (Gradient, translateX-Sweep mit steps() für Glitch-Ruckeln), RGB-Kopie als ::after mit mix-blend-mode und kurzer Opacity.'],
  ['Radial Context Menu', 'Rechtsklick (oder Long-Press) öffnet um den Pointer ein radiales Gold-Menü: Die Aktionen fächern sich auf einem Kreis mit Feder-Stagger auf, das Zentrum trägt das Schließen-Kreuz. Tastatur: Pfeiltasten rotieren die Auswahl, Enter bestätigt, ESC schließt — `role="menu"` sauber verdrahtet.', 'JS positioniert die Items per `transform: rotate(a) translate(r) rotate(-a)` um den Pointer (einmalig beim Öffnen, kein rAF); CSS-Transition mit `--i`-Stagger + Feder-Easing (cubic-bezier mit Overshoot). Zentrum = Schließen. Fokus-Falle im Menü, `aria-activedescendant` oder Pfeiltasten-Rotation; Klick außerhalb/ESC schließt. Position wird gegen Viewport-Ränder geclampt.'],
  ['Scan Focus Text Reveal', 'Text liegt blurrig im Hintergrund; ein Sucher/Viewfinder streicht darüber und zieht die Zeichen in die Schärfe — präziser, maschineller Reveal für Hero-Headlines.', 'JS+CSS: Zeichenweise blur-Maske — ein Gradient (scharfer Kern) fährt mit transform über den Text; blur() pro Zeichen via CSS-Variable, getBoundingClientRect für Trigger.'],
  ['Scratch Gold Reveal', 'Eine Goldfolie (deterministisches Glitter-/Noise-Muster auf Canvas) bedeckt den Content; der User kratzt sie mit Pointer/Touch frei. Ab einem Schwellwert zerfällt die Folie mit einem Gold-Funken-Burst — Rubbellos-Premium für Promos, Coupons und „Verdeckte Features".', 'Canvas 2D-Overlay: Folie = Gold-Verlauf + deterministisches Glitter (det()-PRNG-Punkte) + leichte Textur. Kratzen: Pointer-Events zeichnen radiale Löcher mit `globalCompositeOperation: "destination-out"`. Fortschritt = erfasste Pixel / Gesamtfläche (alle ~8 Frames stichprobenartig). Ab `revealThreshold` blendet das Canvas mit `opacity`-Transition aus und ein deterministischer Funken-Burst (det()-Positionen) feuert einmalig. `aria-hidden` auf dem Canvas, Inhalt darunter ist echtes DOM.'],
  ['Scroll Layout Formation', 'Scroll-getriebene „Layout-Assembly": In einer gepinnten Sektion versammeln sich verstreute Karten/Glyphen Stück für Stück zu einer Formation (Raster, Reihen, Kreis, Spalten) — die finale Komposition ist das Ziel der Choreografie. NOX-Variante der Codrops-„On-Scroll Layout Formations".', 'JS+CSS mit Sticky-Pinning: Container `position: sticky` (oder ScrollTrigger-Proxy), Items starten mit deterministischen Offsets (`det(i, salt)` → Zielposition in %) und fahren auf scroll-getriebene Ziel-Transforms (translate/rotate/scale). Alternativ reine CSS-Variante mit `animation-timeline: view()` und `animation-range` pro Item. Formation als Prop wählbar.'],
  ['Sheet Drawer Slide', 'Bottom-Sheet-Drawer mit Feder-Easing: gleitet von unten herauf, Snap-Points bei 25/50/75 %, Swipe-to-Dismiss, Backdrop-Blur — Gold-Griffleiste, nativ via `<dialog>` + CSS `scroll-snap`. NOX-Variante als React-Komponente.', 'JS: `<dialog showModal()>` für Fokus-Trap + Escape nativ; CSS `scroll-snap` auf der Sheet-Inhaltsfläche erzeugt die Snap-Points (75/50/25 %), `overscroll-behavior: contain` verhindert Hintergrund-Scroll. Swipe-to-Dismiss via passiven `pointer`-Listenern (nur Start/Bewegung/Ende — kein rAF-Loop; Bewegung via `transform` direkt). Enter = `translateY(100%) → 0` mit Feder-Cubic-Bezier; Backdrop = `backdrop-filter: blur` + Fade. Alles Transform/Opacity — außer dem einmaligen Backdrop-Blur.'],
  ['Slot Reel Text Roll', 'Headline-Zeichen rollen wie Slot-Machine-Rollen aus dem Zufall in den finalen Text: Jeder Buchstabe hat eine eigene vertikale Rolle, stoppt deterministisch (det()-Offset) und rastet mit einem Feder-Settle ein — das finale Wort leuchtet gold auf.', 'CSS-only: Jeder Buchstabe liegt in einer Maske (`overflow: hidden`, fester `height`); eine innere Rolle (`span.reel`) fährt per `translateY` von einem deterministischen Start-Offset (det()-PRNG, negativ/positiv) auf `0` — mit `cubic-bezier` + kleinem Overshoot. Start-Offsets und Delays kommen aus `det(i, salt)`, damit jeder Buchstabe anders rollt, aber bei gleichem Text identisch bleibt. Kein JS pro Frame.'],
  ['Sorting Visualizer', 'Sortier-Algorithmen als deterministische Gold-Balken-Show: Bubble/Quick/Merge sortieren live mit det()-Startdaten und einstellbarem Schritt-Takt — Datenprozesse werden zur Choreografie (System-/Debug-Ästhetik).', 'JS+CSS: Balken-Höhen aus det()-PRNG-Array (kein `Math.random`), Algorithmus erzeugt vorab eine Schritt-Liste (Vergleich/Swap/Mark), Abspielen per Takt-Intervall; aktive Vergleiche gold, fertig = grün-Gold. Kein per-Frame-Rendering — nur Tausch-Commits via `transform: scaleY`.'],
  ['Split & Sliced Text Decomposition', 'Zwei Zerlegungs-Varianten in einem Effekt: Text spaltet sich hover-bedingt in zwei Hälften ODER in mehrere horizontale Scheiben, die gestaffelt auseinander gleiten.', 'CSS-only: Hälften via overflow-clip + translateX(-/+) auf ::before/::after; Scheiben-Variante mit N Kopien und clip-path-inset je Streifen, translateY-Offsets per nth-child.'],
    ['Step Flow Wizard', 'Mehrstufiges Formular als Gold-Wizard: Die Fortschrittslinie zeichnet sich Schritt für Schritt, Connector-Kreise füllen sich mit Feder-Easing, der nächste Schritt fährt ein — Orientierung ohne Hektik (NOX-Variante des Multi-Step-Formulars).', 'JS+CSS: Steps als Liste mit `::before`-Connector; Zustände via `[data-state=done|active|todo]`; Linien-Fortschritt als `transform: scaleX` (Compositor), Panel-Wechsel als translateX/opacity-Transition; aktiver Schritt gold, erledigte Schritte mit Check-Icon. Zustand in React-State, kein Router nötig.'],
  ['Strikethrough Explain Modal', 'Erklär-Modal mit Durchstreich-Animation: Falsche Antwort wird mit Gold-Strich durchgestrichen, danach klappt die richtige Erklärung auf.', 'CSS + minimal JS: Strikethrough als animierte Linie (scaleX + skew), Modal-Panel mit Spring-Entrance, aria-live für Ergebnis.'],
  ['Strobe Light Text', 'Hartes Strobe-Blinken für Text: kurze Weiß-Gold-Blitz-Salven mit abklingender Frequenz — Aufmerksamkeits-Moment für Alerts und Countdowns.', 'CSS-only: opacity-Keyframes mit steps() für harte Blitze; Frequenz-Rampen über zwei verschachtelte Animationen (schnelle Salve → langsames Nachglühen).'],
  ['SVG Mascot Pulse', 'Ein animiertes SVG-Maskottchen („NOX Sentinel") mit Wellen-/Puls-Animation: Arme/Aura schwingen in überlagerten Sinuswellen, beim Hover reagiert es mit einem Aufmerksamkeits-Puls, beim Klick mit einem Bestätigungs-Nicken. NOX-Variante des Reverse-Engineerings der Claude-AI-Mascot-Animationen.', 'SVG + CSS-Animationen: Arme/Aura als Pfade mit `transform-origin` an den Schultern; Wellenbewegung durch überlagerte Sinus-Komponenten (`calc()` mit `sin()` in CSS oder deterministische Phasen via `det(i, salt)` als CSS-Vars). Hover/Klick schalten Klassen mit Federtransitionen. Kein JS pro Frame — alles CSS-Keyframes auf transform/opacity.'],
  ['Tearing Photo Drag', 'Bild zerreißt beim Ziehen in Gold-Splitter: Drag-Offset reißt das Foto vertikal auf, Fragmente fallen mit deterministischem PRNG.', 'Canvas + Drag-Interaktion: Tear-Offset aus Pointer-Velocity, Splitter-Pfade via det(i,salt)-PRNG, Gold-Splitter-Overlay auf dunklem Foto.'],
  ['Terminal Typewriter Hover', 'Log-/Terminal-Zeilen: Beim Hover blinkt der Cursor und die Zeile ‚tippt\' sich Buchstabe für Buchstabe nach — Daten-Sektionen werden lebendig.', 'Zeile als data-text; Hover startet CSS-Stepper-Animation auf width/characters (ch-Einheit) mit steps(), Cursor blinkt separat. Kein JS-Intervall; prefers-reduced-motion: sofort voll.'],
  ['Three.js Particle Text Reveal', 'Headline erscheint aus einer Partikel-Wolke: Tausende Punkte formen sich per drei.js zum Text (Canvas-Text als Target-Map) und explodieren beim Hover wieder — Premium-Statement.', 'three.js: Text via Canvas-Text → Pixel-Sampling (hell = Partikel), Points-Instancing mit Positionen; Transition animiert den Mix-Faktor (Wolke→Text), Hover stört lokal mit Sinus.'],
  ['Typewriter Command Palette', 'Cmd+K-Palette im Terminal-Look: Eingabe tippt in Monospace, Vorschläge erscheinen gestaffelt mit Cursor-Flair, aktiver Treffer gold-markiert (Fuzzy-Highlight).', 'Modal mit backdrop-blur; Eingabezeile + Ergebnisliste; Stagger-Entry via transition-delay (det-PRNG), Fuzzy-Match-Highlight in Gold (mark). Kein externes Fuzzy-Lib — kleiner Score-Snippet.'],
  ['Vertical Slit Slideshow', 'Slideshow mit vertikalen Gold-Slits: Beim Wechsel zerfällt das Bild in Streifen, die gestaffelt kippen und das nächste freigeben.', 'CSS 3D + JS-Steuerung: N Slit-Streifen mit translateZ/RotateX, Stagger-Delays, Gold-Kanten-Licht.'],
  ['View Transition List Filter', 'Filter-Chips sortieren eine Liste: jedes Item trägt eine eigene `view-transition-name`, das DOM-Update läuft in `document.startViewTransition()` — der Browser morpht die Items nativ an ihre neue Position (FLIP ohne Library). Abgrenzung zu [[View Transition API Cross-Fade]]: dort Zustands-Crossfade, hier Item-Reorder-Choreografie.', 'Jedes List-Item bekommt eine deterministisch generierte `view-transition-name` (aus der Item-ID, sanitized — nie `index` als Name, sonst vertauschen sich Snapshot-Zuordnungen bei Reorder). Der Filter-Click ruft `document.startViewTransition(() => setFilter(f))` auf; der Browser snapshotet alte und neue Item-Positionen und animiert sie (natives FLIP). Eintritts-Animation neuer Items über `::view-transition-new(...)`-Keyframes; `prefers-reduced-motion`-Guard + Feature-Detect (Fallback: direkter Update).'],
  ['Warp Tunnel Depth', '⛔ **DUPLIKAT** — Kern-Mechanik (Gold-Streaks radial aus der Bildmitte, scroll-reaktiv, Tiefenstaffelung) ist bereits als Default-Variante `section-warp` in **NoxStarfieldDrift** umgesetzt. Batch-2-Review 08.08.2026: nicht bauen, stattdessen Verweis hier. Ersatzkandidat war Star Rating Gold Pop (forms, gebaut).', 'CSS Scroll-Driven Animations: Streaks sind DOM-Elemente (oder ein Canvas), deren `--speed`-CSS-Variable an `animation-timeline: scroll()` gekoppelt ist — Scroll-Tempo übersetzt sich direkt in Streak-Länge/Geschwindigkeit, kein rAF. Positionen/Winkel deterministisch via det()-PRNG (bei statischem DOM als CSS-Vars gerendert). Drei Ebenen (nah/mittel/fern) mit `scale` + unterschiedlichen `animation-range`s für Tiefe. Canvas-Variante: nur bei sehr vielen Streaks, rAF dann nur während aktiven Scrollens.'],
  ['WebGL Water Distortion Slider', 'Bild-Slider, bei dem Ziehen eine Wasser-Verzerrung auslöst: das aktuelle Bild entsättigt sich und wellt sich hinter dem neu hereinziehenden — Premium-Galerie-Feeling.', 'WebGL (Shader): Texturen beider Bilder, Displacement via fbm-Rauschen das mit Drag-Vektor skaliert; Übergang = Progress von 0→1 mit easeOut; Fallback: CSS-Blur-Slide.'],
  ['WebGPU Dust Dissolve Text', 'MSDF-Headline löst sich beim Scrollen in einen deterministischen Gold-Staub auf und setzt sich wieder zusammen — WebGPU/TSL-Showcase für Studio-Sektionen.', 'WebGPU + TSL (Three.js): MSDF-Text als Partikelquelle, Dissolve-Fortschritt via Uniform; Partikelpositionen aus det(i, salt)-PRNG vorberechnet. Fallback: CSS-Filter-Version (blur+opacity) ohne Partikel.'],
  ['Word Fade Reading', 'Beim Scrollen leuchten die Wörter eines Absatzes nacheinander auf: Scroll-Fortschritt = Lesefortschritt, jedes Wort wird einzeln gold-gefüllt — Editorial-Storytelling mit klarer Tempo-Kontrolle (NOX-Variante des Scrollytelling-Wort-Fades).', 'JS+CSS: Wörter einmalig in Spans splitten; Scroll-Fortschritt des Absatzes via IntersectionObserver + rAF-Throttle (nur bei Scroll-Events); Opacity/Farbe je Wort aus `Fortschritt × Wortindex` (linear, kein Zufall). Nur opacity/color animiert, keine Layout-Änderung.'],
    ] as const;

const slug = (name: string) => name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');

/** Provenance for concepts intentionally removed from the visible gallery after consolidation. */
export const CONSOLIDATED_CONCEPTS: Partial<Record<(typeof entries)[number][0], string>> = {
  'Magnetic Field Cards': 'cursor-magnetic-cta',
};

export const CONCEPTS_CATALOG: EffectEntry[] = entries
  .filter(([name]) => !CONSOLIDATED_CONCEPTS[name])
  .map(([name, description, technicalBasis], tone) => ({
    meta: {
      id: `concept-${slug(name)}`,
      name: name.replace(/[^A-Za-z0-9]/g, ''),
      displayName: name,
      category: 'concepts',
      sourceWebsite: 'nox-original',
      sourceFiles: [`NOX-Arsenal_Neue-Effekte_2026-08-01-02.zip / Effekte/${name}.md`],
      mode: 'nox-concept',
      complexity: technicalBasis.includes('WebGL') || technicalBasis.includes('GSAP') ? 'high' : 'medium',
      dependencies: [],
      bestFor: ['Experimentelle Motion- und Interaktionsprototypen'],
      performanceNotes: 'Interaktive Prototyp-Preview: Produktions-Performance noch nicht validiert.',
      mobileNotes: 'Mobile-Verhalten vor Produktion separat verifizieren.',
      reducedMotionNotes: 'Statische, vollständig lesbare Endansicht ist vorgesehen.',
      description,
      technicalBasis,
      importPath: '@/motion-arsenal/effects/concepts',
      usageJsx: `<${name.replace(/[^A-Za-z0-9]/g, '')} />`,
      props: [],
      updatedAt: '2026-08-02T10:09:00.000Z',
      improvementStatus: 'needs-review',
      improvementVersion: '0.1.0',
      productionSafe: false,
      status: 'experimental',
    },
    Component: () => <ConceptEffectPreview kind={slug(name)} name={name} tone={tone} />,
  }));
